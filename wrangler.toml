name = "academic-doc-generator"
main = "worker.js" # 这里需要一个空的 worker 脚本，或者干脆不填 main，取决于 wrangler 版本
compatibility_date = "2024-11-28"

# 关键配置：告诉 Workers 静态文件在哪里
[assets]
directory = "./dist" # 或者 .vitepress/dist，取决于你的构建输出

binding = "ASSETS"
